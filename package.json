{
  "scripts": {
    "start": "npm run clean && eleventy --serve",
    "build": "npm run clean && eleventy",
    "clean": "rm -rf ./public"
  },
  "dependencies": {
    "@11ty/eleventy": "^1.0.0",
    "dotenv": "^16.0.0",
    "nft.storage": "^6.0.2",
    "web3.storage": "^3.5.7"
  }
}
